# Tweak Packs

`PR-005` introduces the first live-editable tweak packs for both the developer UI shell and the
headless scenario runner.

Current packs:

- `tweak_packs/release/hero_forest.ron`: canonical bootstrap hero-look overrides covering every
  namespace the tweak registry exposes.
